[project]
name = "rd-replay-evals"
version = "1.0.0"
description = "Stream recorder + golden trace replay engine"
requires-python = ">=3.12"
dependencies = [
    "rd-agent-contracts",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/rd_replay_evals"]
